# 样式架构说明

## 📁 目录结构

```
styles/
├── ARCHITECTURE.md          # 架构设计文档
├── README.md                # 本文件
│
├── base/                    # 基础样式层
│   ├── variables.css        # CSS 变量（设计令牌）
│   ├── reset.css            # 重置样式
│   ├── typography.css       # 字体样式
│   └── scrollbar.css        # 滚动条样式
│
├── components/              # 组件样式层（Ant Design 覆盖）
│   ├── antd-overrides.css   # Ant Design 全局覆盖
│   ├── table.css            # 表格样式
│   ├── form.css             # 表单样式
│   ├── button.css           # 按钮样式
│   ├── card.css             # 卡片样式
│   ├── modal.css            # 模态框样式
│   └── other.css            # 其他组件样式
│
├── layouts/                 # 布局样式层
│   ├── main-layout.css      # 主布局入口（导入其他布局文件）
│   ├── sidebar.css          # 侧边栏样式
│   ├── header.css           # 头部样式
│   ├── content.css          # 内容区样式
│   ├── page-container.css   # 页面容器样式
│   └── multi-tabs.css        # 多标签页样式
│
├── utilities/               # 工具类样式层
│   ├── animations.css       # 动画工具类
│   └── transitions.css      # 过渡效果
│
└── features/                # 功能模块样式层（按需加载）
    ├── auth/
    │   └── login.css
    └── employees/
        └── employee-form.css
```

## 🎯 使用指南

### 全局样式

全局样式通过 `src/index.css` 统一导入，无需手动引入。

### 组件样式

组件样式在组件文件内按需导入：

```tsx
// 布局组件
import '../styles/layouts/main-layout.css'

// 功能模块样式
import '../../../styles/features/auth/login.css'
```

### 添加新样式

1. **基础样式**：添加到 `base/` 目录
2. **组件覆盖**：添加到 `components/` 目录
3. **布局样式**：添加到 `layouts/` 目录
4. **工具类**：添加到 `utilities/` 目录
5. **功能样式**：添加到 `features/` 目录

## 📝 命名规范

- 使用 BEM 命名约定（如 `.main-layout__sidebar`）
- 使用命名空间前缀（如 `.login-*`, `.page-*`）
- Ant Design 覆盖使用 `.ant-*` 前缀

详细规范请参考 `ARCHITECTURE.md`。
