# 样式架构设计文档

## 📐 架构原则

1. **分层清晰**：基础层 → 组件层 → 布局层 → 页面层
2. **职责单一**：每个文件只负责一个功能模块
3. **命名规范**：使用 BEM 命名约定，避免冲突
4. **可维护性**：易于查找、修改和扩展

---

## 📁 目录结构

```
src/styles/
├── ARCHITECTURE.md          # 架构文档（本文件）
├── index.css                # 样式入口文件
│
├── base/                    # 基础样式层
│   ├── reset.css           # 重置样式（从 index.css 提取）
│   ├── variables.css        # CSS 变量（设计令牌）
│   ├── typography.css       # 字体样式
│   └── scrollbar.css        # 滚动条样式
│
├── components/              # 组件样式层（Ant Design 覆盖）
│   ├── antd-overrides.css   # Ant Design 全局覆盖
│   ├── table.css            # 表格样式
│   ├── form.css             # 表单样式
│   ├── button.css           # 按钮样式
│   ├── card.css             # 卡片样式
│   ├── modal.css            # 模态框样式
│   └── ...                  # 其他组件样式
│
├── layouts/                 # 布局样式层
│   ├── main-layout.css      # 主布局入口
│   ├── sidebar.css          # 侧边栏样式
│   ├── header.css           # 头部样式
│   ├── content.css          # 内容区样式
│   └── page-container.css   # 页面容器样式
│
├── utilities/               # 工具类样式层
│   ├── animations.css       # 动画工具类
│   ├── transitions.css     # 过渡效果
│   └── helpers.css          # 辅助类（如 .clearfix）
│
└── features/                # 功能模块样式层（按需加载）
    ├── auth/
    │   └── login.css
    └── employees/
        └── employee-form.css
```

---

## 🎯 样式层次说明

### 1. Base Layer（基础层）
**职责**：定义全局基础样式和设计令牌

- `reset.css`：重置浏览器默认样式
- `variables.css`：CSS 变量（颜色、间距、阴影等）
- `typography.css`：字体、行高、文本样式
- `scrollbar.css`：自定义滚动条

**特点**：
- ✅ 不包含任何业务逻辑
- ✅ 可被所有层引用
- ✅ 修改影响全局

### 2. Components Layer（组件层）
**职责**：覆盖和增强 Ant Design 组件样式

- `antd-overrides.css`：全局覆盖
- `table.css`、`form.css` 等：特定组件样式

**特点**：
- ✅ 只覆盖 Ant Design 组件
- ✅ 使用深度选择器（`.ant-*`）
- ✅ 保持与主题配置一致

### 3. Layouts Layer（布局层）
**职责**：定义页面布局结构样式

- `main-layout.css`：主布局入口（导入其他布局文件）
- `sidebar.css`：侧边栏样式
- `header.css`：头部样式
- `content.css`：内容区样式

**特点**：
- ✅ 使用 BEM 命名（如 `.main-layout__sidebar`）
- ✅ 文件拆分，职责单一
- ✅ 可复用性强

### 4. Utilities Layer（工具层）
**职责**：提供可复用的工具类和动画

- `animations.css`：动画关键帧和类
- `transitions.css`：过渡效果
- `helpers.css`：辅助工具类

**特点**：
- ✅ 纯工具类，无副作用
- ✅ 可在任何地方使用
- ✅ 命名清晰（如 `.animate-fade-in`）

### 5. Features Layer（功能层）
**职责**：特定功能模块的样式

- `features/auth/login.css`：登录页样式
- `features/employees/employee-form.css`：员工表单样式

**特点**：
- ✅ 按功能模块组织
- ✅ 使用命名空间（如 `.login-*`）
- ✅ 组件内按需导入

---

## 🏷️ 命名规范

### BEM 命名约定

```css
/* Block（块） */
.main-layout { }

/* Element（元素） */
.main-layout__sidebar { }
.main-layout__header { }
.main-layout__content { }

/* Modifier（修饰符） */
.main-layout__sidebar--collapsed { }
.main-layout__header--sticky { }
```

### 命名空间约定

| 前缀 | 用途 | 示例 |
|------|------|------|
| `.main-layout-*` | 主布局 | `.main-layout__sidebar` |
| `.page-*` | 页面容器 | `.page-container` |
| `.login-*` | 登录页 | `.login-card` |
| `.employee-form-*` | 员工表单 | `.employee-form__phone-input` |
| `.ant-*` | Ant Design 覆盖 | `.ant-table-wrapper` |

### 工具类命名

```css
/* 动画 */
.animate-fade-in { }
.animate-slide-left { }

/* 过渡 */
.transition-base { }
.transition-fast { }

/* 辅助 */
.clearfix { }
.sr-only { }
```

---

## 📦 导入顺序

### 全局样式入口（`index.css`）

```css
/* 1. 基础层 */
@import './base/variables.css';
@import './base/reset.css';
@import './base/typography.css';
@import './base/scrollbar.css';

/* 2. 组件层 */
@import './components/antd-overrides.css';
@import './components/table.css';
@import './components/form.css';
/* ... 其他组件 */

/* 3. 布局层 */
@import './layouts/main-layout.css';

/* 4. 工具层 */
@import './utilities/animations.css';
@import './utilities/transitions.css';

/* 5. 第三方样式 */
@import 'nprogress/nprogress.css';
```

### 组件内导入（按需）

```tsx
// 组件文件
import './login.css'  // 功能模块样式
```

---

## 🔄 迁移计划

### 阶段 1：创建新结构
- [x] 创建目录结构
- [ ] 拆分 `index.css` → `base/` 目录
- [ ] 拆分 `components.css` → `components/` 目录
- [ ] 拆分 `MainLayout.css` → `layouts/` 目录

### 阶段 2：重构文件
- [ ] 重命名类名（BEM 规范）
- [ ] 更新组件导入路径
- [ ] 测试样式是否正常

### 阶段 3：优化
- [ ] 提取公共样式
- [ ] 优化选择器性能
- [ ] 添加注释文档

---

## 📝 文件大小建议

| 文件类型 | 建议行数 | 说明 |
|----------|---------|------|
| 基础文件 | < 100 | 变量、重置等 |
| 组件文件 | < 200 | 单个组件覆盖 |
| 布局文件 | < 300 | 单个布局模块 |
| 工具文件 | < 150 | 工具类和动画 |

**原则**：超过 300 行考虑拆分

---

## ✅ 最佳实践

1. **避免深层嵌套**：选择器深度不超过 3 层
2. **使用 CSS 变量**：颜色、间距等使用变量
3. **避免 !important**：优先使用选择器优先级
4. **注释清晰**：每个文件添加头部注释
5. **响应式优先**：移动端优先设计

---

## 🚀 未来优化方向

1. **CSS Modules**：避免类名冲突
2. **PostCSS**：增强功能（嵌套、自动前缀）
3. **样式 Lint**：使用 stylelint 规范代码
4. **主题系统**：完善暗色模式支持

---

*最后更新：2024年*
